import React from "react";
import { View, Text, StyleSheet } from "react-native";

const AddSheetScreen = (props) => {
  return (
    <View style={styles.screen}>
      <Text>AddSheet Screen!</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  screen: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  title: {
    fontSize: 18,
    margin: 20,
    textAlign: "center",
    fontWeight: "bold",
  },
  filterContainer: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    width: "80%",
    marginVertical: 15,
  },
});

export default AddSheetScreen;
